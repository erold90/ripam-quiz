### 22:54
<!-- session:4d7306ce-1909-4fae-83de-b58052e07562 turn:e09494b6-184b-4c72-8de6-d02230d3a272 transcript:/Users/witerose/.claude/projects/-Users-witerose/4d7306ce-1909-4fae-83de-b58052e07562.jsonl -->
- L'utente ha fornito un token Supabase (sbp_27dcb466...) per migrare il progetto RIPAMQuiz da un vecchio progetto bannato a uno nuovo.

- Claude ha verificato Supabase CLI (v2.84.2), collegato il progetto al nuovo ref `rfdmoqbtoctrnoazmlmc` e recuperato le API keys (anon e service_role) tramite Management API.

- Claude ha creato una migrazione SQL (supabase/migrations/20260501230000_initial_schema.sql) con 5 tabelle, ha corretto un errore di funzione UUID (`uuid_generate_v4()` → `gen_random_uuid()`), e ha applicato lo schema al DB remoto via `supabase db push`.

- Claude ha abilitato auth anonima via Management API, eseguito un smoke test (insert/delete su `user_progress` con anon key) con esito positivo (HTTP 201/204), e verificato che il RLS fosse disabilitato.

- Claude ha aggiornato `.env.local` e il fallback in `src/lib/supabase.ts` con le nuove coordinate, ha rimosso tutti i riferimenti al vecchio progetto, ha buildato Next.js (26 pagine statiche) e ha deployato su Cloudflare Pages (`ripamquiz.pages.dev`) con verifiche di HTTP 200 e corretto URL nei chunk JS.

- Claude ha aggiornato la memoria MCP aggiungendo osservazioni su `RIPAMQuiz_Database` con il nuovo project ref, causa del ban (email temp-mail), schema ricreato, e notazione della perdita definitiva dello storico (2792 risposte di Sara).

### 23:01
<!-- session:4d7306ce-1909-4fae-83de-b58052e07562 turn:c20afdad-a157-4282-b9d8-63bee9587a3c transcript:/Users/witerose/.claude/projects/-Users-witerose/4d7306ce-1909-4fae-83de-b58052e07562.jsonl -->
- L'utente ha richiesto un audit generale completo con tutti i test necessari per verificare il funzionamento dell'app RIPAMQuiz.

- Claude ha creato una task list di 6 task (verifica schema DB, CRUD test, auth+CORS, routes Cloudflare, dati statici quiz, linting) e ha eseguito audit end-to-end parallelizzando i check.

- Claude ha verificato l'integrità dello schema DB Supabase (`rfdmoqbtoctrnoazmlmc`) via Management API: 5/5 tabelle presenti (user_progress, user_stats, simulazioni, dispense_highlights, reading_progress) con colonne, tipi, 14 indici, constraint UNIQUE allineati agli onConflict del codice, RLS OFF, grant `anon` completi, auth anonima abilitata, CORS `*` configurato.

- Claude ha eseguito test CRUD completi su anon key Supabase su tutte le 5 tabelle con INSERT (201), UPSERT onConflict (200), SELECT (200), DELETE (204); idempotenza confermata con riutilizzo dello stesso ID.

- Claude ha testato il deploy su Cloudflare Pages (ripamquiz.pages.dev) verificando 26 route totali (home 200, 7 principali + 13 quiz dinamiche + 3 dispense con 308→200 redirect), bundle JS contenente nuovo project ref Supabase e nessun riferimento al vecchio, asset PWA (manifest, sw.js, favicon) raggiungibili.

- Claude ha verificato i 13 JSON quiz (totale 12.132 quiz identico allo storico) e 3 EPUB dispense accessibili via HTTPS, eseguito TypeScript check (exit 0) e ESLint (0 errori, 7 warning preesistenti), simulato E2E con UUID Sara confermando GET/POST/DELETE su nuovo DB con HTTP 200/201/204 come atteso.

